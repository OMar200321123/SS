name: Deploy To Development Server

on:
  push:
    branches:
    - dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest #test_deploy_v1
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'

    - name: Install dependencies
      run: npm install

    - name: Build application
      run: npm run build
      env:
        CI: true
        VITE_API_BASE_URL: ${{ secrets.DEV_VITE_API_BASE_URL }}
        VITE_SECRET_KEY: ${{ secrets.DEV_VITE_SECRET_KEY }}

    - name: Remove the dist folder from VPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.DEV_VPS_HOST }}
        username: ${{ secrets.DEV_VPS_USERNAME }}
        password: ${{ secrets.DEV_VPS_PASSWORD }}
        script: |
          cd ${{ secrets.DEV_APP_PATH }}
          rm -r dist
    
    - name: Deploy To VPS
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.DEV_VPS_HOST }}
        username: ${{ secrets.DEV_VPS_USERNAME }}
        password: ${{ secrets.DEV_VPS_PASSWORD }}
        source: "dist/"
        target: ${{ secrets.DEV_APP_PATH }}
